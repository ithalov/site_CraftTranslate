import { useEffect, useMemo, useState } from 'react';
import { Badge } from '@/components/ui/Badge';
import { Card } from '@/components/ui/Card';
import { MetricCard } from '@/components/ui/MetricCard';
import { ProgressBar } from '@/components/ui/ProgressBar';
import { RouteLoadingScreen } from '@/components/layout/RouteLoadingScreen';
import { useLocale } from '@/hooks/useLocale';
import { fetchPublicStatusData, type PublicStatusData } from '@/services/publicStatus';

function languageLabel(language: PublicStatusData['languages'][number]) {
  return `${language.code} - ${language.name}`;
}

export function StatusPage() {
  const { locale } = useLocale();
  const [data, setData] = useState<PublicStatusData | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);
  const copy = {
    'pt-BR': { eyebrow: 'Status publico', title: <>A traducao esta<br /><span className="text-[#c7f464]">em movimento.</span></>, lead: 'Acompanhe o progresso da comunidade sem expor mensagens, chaves ou notas privadas.', live: 'Dados publicos em tempo real', protected: 'Conteudo protegido', updated: 'Atualizado pelo Supabase', snapshot: 'Panorama de cobertura', translated: 'Traduzido', reviewed: 'Revisado', official: 'Oficial', metrics: [['Strings', 'chaves de traducao'], ['Idiomas', 'comunidades ativas'], ['Colaboradores', 'perfis publicos'], ['Traducoes', 'sugestoes enviadas'], ['Revisoes', 'checagens de qualidade'], ['Cobertura oficial', 'versoes aprovadas']], coverage: 'Cobertura por idioma', coverageLead: 'Cada barra representa apenas dados agregados. O texto das strings permanece privado.', strings: 'Strings', approved: 'Aprovadas', empty: 'Nenhum idioma ativo foi encontrado ainda.', error: 'Nao foi possivel carregar o status do projeto.' },
    en: { eyebrow: 'Public status', title: <>Translation is<br /><span className="text-[#c7f464]">in motion.</span></>, lead: 'Follow community progress without exposing messages, keys, or private notes.', live: 'Live public data', protected: 'Protected content', updated: 'Updated by Supabase', snapshot: 'Coverage snapshot', translated: 'Translated', reviewed: 'Reviewed', official: 'Official', metrics: [['Strings', 'translation keys'], ['Languages', 'active communities'], ['Contributors', 'public profiles'], ['Translations', 'submitted suggestions'], ['Reviews', 'quality checks'], ['Official coverage', 'approved versions']], coverage: 'Coverage by language', coverageLead: 'Each bar represents aggregate data only. String text stays private.', strings: 'Strings', approved: 'Approved', empty: 'No active languages were found yet.', error: 'Unable to load project status.' },
    es: { eyebrow: 'Estado publico', title: <>La traduccion esta<br /><span className="text-[#c7f464]">en movimiento.</span></>, lead: 'Sigue el progreso de la comunidad sin exponer mensajes, claves ni notas privadas.', live: 'Datos publicos en vivo', protected: 'Contenido protegido', updated: 'Actualizado por Supabase', snapshot: 'Resumen de cobertura', translated: 'Traducido', reviewed: 'Revisado', official: 'Oficial', metrics: [['Strings', 'claves de traduccion'], ['Idiomas', 'comunidades activas'], ['Colaboradores', 'perfiles publicos'], ['Traducciones', 'sugerencias enviadas'], ['Revisiones', 'controles de calidad'], ['Cobertura oficial', 'versiones aprobadas']], coverage: 'Cobertura por idioma', coverageLead: 'Cada barra representa solo datos agregados. El texto permanece privado.', strings: 'Strings', approved: 'Aprobadas', empty: 'Todavia no se encontraron idiomas activos.', error: 'No se pudo cargar el estado del proyecto.' }
  }[locale];

  useEffect(() => { let active = true; void fetchPublicStatusData().then((result) => { if (active) setData(result); }).catch((loadError: unknown) => { if (active) setError(loadError instanceof Error ? loadError.message : copy.error); }).finally(() => { if (active) setLoading(false); }); return () => { active = false; }; }, [copy.error]);

  const summary = data?.summary ?? { total_strings: 0, total_languages: 0, collaborators: 0, total_translations: 0, total_reviews: 0, translated_percent: 0, reviewed_percent: 0, official_percent: 0 };
  const languages = useMemo(() => data?.languages.slice(0, 6) ?? [], [data]);
  const metricValues = [summary.total_strings, summary.total_languages, summary.collaborators, summary.total_translations, summary.total_reviews, `${summary.official_percent}%`];
  const formatter = new Intl.NumberFormat(locale);

  if (loading) return <RouteLoadingScreen />;
  if (error) return <section className="grid min-h-[60vh] place-items-center"><Card className="max-w-lg p-8 text-center"><p className="pixel-label text-[10px] text-[#566172]">ChatTranslate</p><h1 className="minecraft-title mt-3 text-3xl">{copy.error}</h1><p className="mt-4 text-sm text-[#566172]">{error}</p></Card></section>;

  return <section className="space-y-6 pb-12"><Card className="relative overflow-hidden bg-[#101114] p-0 text-white"><div className="absolute inset-0 bg-[radial-gradient(circle_at_10%_15%,rgba(199,244,100,.22),transparent_24%),radial-gradient(circle_at_88%_85%,rgba(76,201,240,.2),transparent_32%)]" /><div className="relative grid gap-8 px-6 py-9 md:px-10 md:py-12 lg:grid-cols-[1.1fr_.9fr] lg:items-center"><div><p className="pixel-label text-[10px] text-[#c7f464]">{copy.eyebrow}</p><h1 className="minecraft-title mt-4 text-4xl leading-[.92] md:text-6xl">{copy.title}</h1><p className="mt-5 max-w-xl text-sm leading-7 text-white/75 md:text-base">{copy.lead}</p><div className="mt-7 flex flex-wrap gap-2"><Badge tone="neutral" className="border-white/15 bg-white/10 text-white">{copy.live}</Badge><Badge tone="neutral" className="border-white/15 bg-white/10 text-white">{copy.protected}</Badge><Badge tone="neutral" className="border-white/15 bg-white/10 text-white">{copy.updated}</Badge></div></div><div className="rounded-2xl border border-white/10 bg-white/[.07] p-5 backdrop-blur-sm"><div className="flex items-center justify-between"><p className="pixel-label text-[10px] text-white/60">{copy.snapshot}</p><span className="h-2.5 w-2.5 rounded-full bg-[#c7f464] shadow-[0_0_14px_#c7f464]" /></div><div className="mt-5 grid gap-3 sm:grid-cols-3 lg:grid-cols-1">{[[copy.translated, summary.translated_percent, '#c7f464'], [copy.reviewed, summary.reviewed_percent, '#4cc9f0'], [copy.official, summary.official_percent, '#ffb86b']].map(([label, value, color]) => <div key={label as string} className="rounded-xl border border-white/10 bg-[#0d0f13]/35 p-4"><p className="text-xs text-white/60">{label}</p><p className="mt-1 text-3xl font-extrabold" style={{ color: color as string }}>{value}%</p></div>)}</div></div></div></Card><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{copy.metrics.map(([label, hint], index) => <MetricCard key={label} label={label} value={typeof metricValues[index] === 'number' ? formatter.format(metricValues[index] as number) : metricValues[index] as string} hint={hint} />)}</div><Card className="overflow-hidden p-0"><div className="flex flex-col gap-3 border-b-2 border-[#101114] bg-[#c7f464] px-6 py-4 md:flex-row md:items-center md:justify-between"><div><p className="pixel-label text-[10px] text-[#101114]">{copy.coverage}</p><p className="mt-1 text-sm font-bold text-[#101114]">{copy.coverageLead}</p></div><Badge tone="neutral" className="border-[#101114]/15 bg-white/45 text-[#101114]">{summary.total_languages} {copy.metrics[1][0]}</Badge></div><div className="grid gap-5 p-5 md:p-6 xl:grid-cols-2">{languages.length ? languages.map((language, index) => <article key={language.language_id} className="group rounded-2xl border border-[#dfe3ea] bg-[#f7f8fb] p-5 transition duration-200 hover:-translate-y-1 hover:bg-white hover:shadow-[6px_6px_0_#101114]"><div className="flex items-start justify-between gap-4"><div><p className="font-[var(--font-display)] text-xl font-extrabold text-[#101114]">{languageLabel(language)}</p><p className="mt-1 text-sm text-[#566172]">{language.native_name}</p></div><Badge tone={index % 2 === 0 ? 'accent' : 'success'}>{language.official_percent}% {copy.official.toLowerCase()}</Badge></div><div className="mt-6 space-y-4"><ProgressBar label={copy.translated} value={language.translated_percent} tone="accent" /><ProgressBar label={copy.reviewed} value={language.reviewed_percent} tone="success" /><ProgressBar label={copy.official} value={language.official_percent} tone="warning" /></div><div className="mt-6 grid grid-cols-2 gap-3"><div className="rounded-xl border border-[#dfe3ea] bg-white p-3.5"><p className="pixel-label text-[9px] text-[#566172]">{copy.strings}</p><p className="mt-2 text-xl font-extrabold text-[#101114]">{formatter.format(Number(language.total_strings ?? 0))}</p></div><div className="rounded-xl border border-[#dfe3ea] bg-white p-3.5"><p className="pixel-label text-[9px] text-[#566172]">{copy.approved}</p><p className="mt-2 text-xl font-extrabold text-[#101114]">{formatter.format(Number(language.official_count ?? 0))}</p></div></div></article>) : <p className="rounded-xl border border-[#dfe3ea] bg-[#f7f8fb] p-6 text-sm text-[#566172] xl:col-span-2">{copy.empty}</p>}</div></Card></section>;
}
